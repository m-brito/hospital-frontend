import { useLogin } from '../Login/hooks/useLogin';
import {
  Container
} from "./styles";


export const Home = () => {
  
  return (
    <Container>
      
    </Container>
  );
};

export default Home;
