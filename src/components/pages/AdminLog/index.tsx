import { useLogin } from '../Login/hooks/useLogin';
import {
  Container
} from "./styles";


export const AdminLog: React.FC = () => {
  
  return (
    <Container>
      
    </Container>
  );
};

export default AdminLog;
